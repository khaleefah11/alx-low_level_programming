#include "main.h"
/**
 *_abs - function to calculate the absolute value of an int
 *@n: The int type parameter the function takes
 *
 * Return: Return the computed absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (-n);
}
