#include <stdio.h>

/* main function with a void parameter */
int main(void) 
{

	/* initializing str */
	char str[] = "\"Programming is like building a multilingual puzzle";

	/* writing to stdout */
	puts(str);

	return (0);
}
